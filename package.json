{
  "name": "mycelia",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "build:wasm": "wasm-pack build crates/hypha-core --target web --out-dir ../../pkg/hypha-core && wasm-pack build crates/sensor-field --target web --out-dir ../../pkg/sensor-field && wasm-pack build crates/evolution --target web --out-dir ../../pkg/evolution",
    "test": "vitest run",
    "test:wasm": "cargo test --workspace",
    "lint": "tsc --noEmit",
    "format": "prettier --write src/"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0",
    "prettier": "^3.3.0"
  },
  "dependencies": {
    "three": "^0.168.0",
    "simple-peer": "^9.11.0",
    "ws": "^8.18.0"
  }
}
